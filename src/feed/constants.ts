export const PAGE_SIZE = 12;             // page size
export const PRELOAD_AHEAD = 2;          // prefetch when within 2 items from end
export const COOLDOWN_RECENT = 10;       // avoid showing items seen in last N appearances
export const MAX_POOL = 600;             // cap in-memory items
export const SEEN_TTL_DAYS = 7;          // how long to remember "seen"